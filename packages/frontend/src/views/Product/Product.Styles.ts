import styled from "@emotion/styled";

export const ProductStyles = styled.div({
  ".paper": {
    marginTop: 10,
    padding: 0,
    borderRadius: 10,
  },
  ".pagination": {
    marginTop: 15,
    marginBottom: 15,
  },
});
